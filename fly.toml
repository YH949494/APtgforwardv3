app = "aptgforwardv3"   # <-- all lowercase, no underscore
primary_region = "sin"

[env]
  PYTHONUNBUFFERED = "1"

[processes]
  app = "python bot.py"

# ❌ Do not include [build] if you already have Dockerfile
